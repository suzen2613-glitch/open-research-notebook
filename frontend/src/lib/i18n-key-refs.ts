// Keep these translation keys referenced so the locale parity/unused-key test
// can detect that they are intentionally reserved for UI wording parity.
export const I18N_KEY_REFERENCES = [
  'common.chat',
  'sources.generateNewInsight',
  'sources.insightsDesc',
] as const;
